// Package model defines the domain models shared across the simulator.
//
// This includes transaction payloads, simulation run configurations,
// source profiles (bank / merchant), and API response types. Models
// carry no framework dependencies and are safe to import from any layer.
package model

