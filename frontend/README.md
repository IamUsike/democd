# Transaction Monitoring frontend

Vite + React + TypeScript placeholder UI.

```bash
npm install
npm run dev
```

Dev server defaults to http://localhost:5173. Backend API defaults to
http://localhost:8081.
